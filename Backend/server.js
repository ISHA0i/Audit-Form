/**
 * Cybersecurity Audit Form Backend
 * 
 * This is the entry point that redirects to the actual server implementation
 */

// Import the server from the src directory
const server = require('./src/server');

// Export the server
module.exports = server; 